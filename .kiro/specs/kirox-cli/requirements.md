# Requirements Document

## Project Description (Input)

以下のコマンドでリポジトリに登録してある以下のファイルを現在のディレクトリに取得してくるコマンド

```bash
npx kirox <github_user>/<repo_name> -p <project>
```

取得対象ファイル:
- `.kiro/specs/<project>**`
- `.kiro/steering/**`

## Requirements
<!-- Will be generated in /kiro:spec-requirements phase -->
