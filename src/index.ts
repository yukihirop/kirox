#!/usr/bin/env node

/**
 * Kirox CLI - Entry Point
 *
 * CLI tool to fetch Kiro specification and steering files from remote GitHub repositories.
 */

console.log('Kirox CLI - Setup complete');
process.exit(0);
